# Short-lived mutual-TLS client leaf — same as tls-client with 30-day validity.
name = "mtls-client"
default_days = 30

[basic_constraints]
ca = false

[key_usage]
critical = true
digital_signature = true

[extended_key_usage]
client_auth = true

[subject_alt_name]
# SECURITY: a CSR-requested subjectAltName is the requester's claim, not a
# verified fact, so it is NOT certified by default. Either list the names
# here (`dns = ["host.example"]`) or, after checking them, pass
# `ca sign-csr -copy-csr-san` for a single issuance.
from_csr = false

[subject_key_identifier]
include = true

[authority_key_identifier]
include = true
